# Levy

A cross-browser WXT extension using React, Tailwind CSS, and shadcn/ui in a side panel.

```sh
npm install
npm run dev
```

Click the extension toolbar icon to open Levy. Chrome uses the Side Panel API;
Firefox uses the Sidebar Action API.
